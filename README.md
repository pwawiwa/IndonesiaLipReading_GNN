# IDLWR Lip Reading (Landmark Extraction)

## Step A: Extract Landmarks
This step processes videos into landmark `.npz` files using **MediaPipe FaceMesh**.

### Usage
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
